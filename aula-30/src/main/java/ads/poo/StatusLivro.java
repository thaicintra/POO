package ads.poo;

/**
 * ENUM - lista constante nomeadas
 * --> Impede valores inválidos (compilador garante)
 * --> Substitui o boolean emprestado
 */
public enum StatusLivro {
    DISPONIVEL,
    EMPRESTADO,
    RESERVADO;
}
